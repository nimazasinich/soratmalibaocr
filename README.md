# 🧠 CloudCoder Financial Fraud Detection & OCR System

**سیستم هوشمند تحلیل صورت‌های مالی و تشخیص تقلب با قابلیت OCR**

## 📋 توضیحات

این پروژه یک سیستم کامل تحلیل مالی است که با استفاده از الگوریتم‌های پیشرفته، صورت‌های مالی را تحلیل کرده و شاخص‌های مالی، ریسک، و احتمال تقلب را محاسبه می‌کند.

### ✨ قابلیت‌های کلیدی

- 📊 **تحلیل نسبت‌های مالی**: محاسبه خودکار 12+ نسبت مالی
- 🕵️ **تشخیص تقلب**: 5 الگوریتم پیشرفته شناسایی تقلب (قانون بنفورد، کیفیت سود، و...)
- 📈 **پیش‌بینی مالی**: مدل Z-Score برای پیش‌بینی ورشکستگی
- 🔍 **OCR دوگانه**: پشتیبانی از OCR سمت سرور و کلاینت برای فارسی و انگلیسی
- 📱 **رابط کاربری فارسی**: داشبورد RTL با طراحی مدرن
- 🔐 **احراز هویت**: سیستم JWT با نقش‌های کاربری
- 📑 **گزارش‌دهی**: خروجی PDF با سرفصل‌های فارسی

## 🏗️ معماری پروژه

```
soratmalibaocr/
├── backend/           # Node.js + TypeScript + Express API
│   ├── src/
│   │   ├── controllers/
│   │   ├── services/
│   │   ├── repositories/
│   │   ├── middlewares/
│   │   ├── ocr/
│   │   └── utils/
│   └── package.json
├── frontend/          # React + TypeScript + Vite
│   ├── src/
│   └── package.json
├── database/          # SQLite database files
└── docker-compose.yml
```

## 🚀 راه‌اندازی سریع

### پیش‌نیازها

- Node.js >= 18.0.0
- npm >= 9.0.0
- Docker & Docker Compose (برای deployment)

### روش 1: اجرا با Docker (توصیه می‌شود) 🐳

```bash
# 1. کلون کردن ریپازیتوری (اگر قبلاً انجام نشده)
git clone <repository-url>
cd soratmalibaocr

# 2. اجرای Docker Compose
docker-compose up -d

# 3. مشاهده لاگ‌ها
docker-compose logs -f
```

✅ **سیستم آماده است!**
- Backend API: http://localhost:8000
- Frontend Dashboard: http://localhost:5173
- API Health: http://localhost:8000/api/health
- API Docs: http://localhost:8000/api/docs (قابل پیاده‌سازی در آینده)

### روش 2: اجرای Manual (برای توسعه)

```bash
# 1. نصب وابستگی‌های root
npm install

# 2. کپی فایل محیطی
cp .env.example .env

# 3. راه‌اندازی Backend
cd backend
npm install
npm run db:init  # Initialize database
npm run dev

# 4. در ترمینال جدید - راه‌اندازی Frontend
cd frontend
npm install
npm run dev
```

Backend در `http://localhost:8000` و Frontend در `http://localhost:5173` اجرا می‌شود.

## 📊 نسبت‌های مالی محاسبه‌شده

| دسته‌بندی | نسبت | فرمول |
|----------|------|-------|
| نقدینگی | نسبت جاری | دارایی‌های جاری / بدهی‌های جاری |
| نقدینگی | نسبت آنی | (دارایی‌های جاری - موجودی) / بدهی‌های جاری |
| اهرمی | نسبت بدهی | کل بدهی / کل دارایی |
| سودآوری | حاشیه سود خالص | سود خالص / فروش |
| سودآوری | بازده دارایی (ROA) | سود خالص / کل دارایی |
| سودآوری | بازده حقوق صاحبان سهام (ROE) | سود خالص / حقوق صاحبان سهام |
| کارایی | گردش دارایی | فروش / کل دارایی |

## 🕵️ شاخص‌های تشخیص تقلب

1. **قانون بنفورد (Benford's Law)**: تحلیل توزیع ارقام اول
2. **کیفیت سود**: مقایسه جریان نقدی با سود خالص
3. **رشد غیرعادی حساب‌های دریافتنی**
4. **تورم دارایی‌های ثابت**
5. **نسبت تعهدی (Accrual Ratio)**

## 🔮 مدل پیش‌بینی ورشکستگی (Altman Z-Score)

```
Z = 1.2*WC/TA + 1.4*RE/TA + 3.3*EBIT/TA + 0.6*MVE/TL + 1.0*Sales/TA
```

- Z > 2.99: منطقه امن
- 1.81 < Z < 2.99: منطقه خاکستری
- Z < 1.81: خطر ورشکستگی

## 📡 API Documentation

مستندات کامل API در آدرس `http://localhost:8000/api/docs` در دسترس است.

## 🧪 تست

```bash
# تست Backend
npm run test:backend

# تست Frontend
npm run test:frontend

# تست E2E
cd frontend && npm run test:e2e
```

## 🛠️ تکنولوژی‌های استفاده شده

### Backend
- Node.js + TypeScript
- Express.js
- SQLite3
- Tesseract.js (OCR)
- JWT Authentication
- Swagger/OpenAPI

### Frontend
- React 18
- TypeScript
- Vite
- TailwindCSS (RTL)
- Chart.js
- i18next (فارسی‌سازی)
- Zustand (State Management)

## 📝 لایسنس

MIT License

## 👨‍💻 توسعه‌دهندگان

ساخته شده با ❤️ توسط CloudCoder AI

---

**نسخه:** 1.0.0
**آخرین به‌روزرسانی:** 1403/08/18
